% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neotoma_taxonomy.R
\name{neotoma_taxonomy}
\alias{neotoma_taxonomy}
\title{Neotoma's taxonomy, fetched from the API}
\usage{
neotoma_taxonomy(taxa_group = NULL, refresh = FALSE, quiet = FALSE)
}
\arguments{
\item{taxa_group}{Optional character vector of Neotoma taxa-group codes to
keep, e.g. \code{"VPL"} for vascular plants or \code{"DIA"} for diatoms. \code{NULL}
(default) keeps every group.}

\item{refresh}{If \code{TRUE}, ignore any cached copy and re-download.}

\item{quiet}{Suppress progress messages.}
}
\value{
A data frame of class \code{tilia_lookup} with columns \code{taxon_id},
\code{name}, \code{author}, \code{code}, \code{taxa_group}, \code{ecol_group} and \code{higher_id},
carrying the synonymy as \code{attr(, "synonyms")} and the fetch time as
\code{attr(, "fetched")}. Returns \code{NULL} invisibly, with a message, if the API
cannot be reached and no cache exists.
}
\description{
Returns Neotoma's taxon list and its synonymy in the same shape as
\code{\link[=read_tilia_lookup]{read_tilia_lookup()}}, so it can be passed to \code{\link[=standardize_dic]{standardize_dic()}}
interchangeably. Use this when Tilia's lookup files are unavailable --
Tilia installs only on Windows, so on macOS and Linux this is the only
route to reconciliation.
}
\details{
Unlike Tilia, which ships a separate lookup file per proxy, Neotoma stores
one taxonomy covering vascular plants, diatoms, ostracodes, phytoliths and
the rest, distinguished by \code{taxa_group}. Filter with \code{taxa_group} to get the
vocabulary for your proxy; the cached copy always holds everything, so
switching proxies costs no further download.
}
\section{Caching}{

The first call downloads the full taxon table -- tens of megabytes, since
the API cannot filter server-side -- and caches a compact version under
\code{\link[tools:R_user_dir]{tools::R_user_dir()}}. Later calls read the cache and are immediate. Pass
\code{refresh = TRUE} to re-download; \code{neotoma_taxonomy_cache()} reports where
the cache lives and how old it is, and can clear it.

Because the taxonomy is live, a reconciliation run today may differ from one
next year. The object records when it was fetched, and printing it shows
that date; cite it if reproducibility matters.
}

\section{Testing hooks}{

\code{options(pcountr.neotoma_api =)} and \code{options(pcountr.cache_dir =)} override
the API base and the cache location. These exist so the package's own tests
can exercise the offline path without a network call or touching a real
cache; they are not intended for normal use.
}

\section{Attribution}{

Neotoma data are made available under CC BY 4.0. If you use this in
published work, cite the database (Williams et al., 2018) alongside
\code{pcountr}.
}

\references{
Williams, J.W., Grimm, E.C., Blois, J.L., et al. (2018). The
Neotoma Paleoecology Database, a multiproxy, international,
community-curated data resource. \emph{Quaternary Research}, 89(1), 156-177.
\doi{10.1017/qua.2017.105}
}
\seealso{
\code{\link[=standardize_dic]{standardize_dic()}}, \code{\link[=read_tilia_lookup]{read_tilia_lookup()}},
\code{\link[=neotoma_taxonomy_cache]{neotoma_taxonomy_cache()}}
}

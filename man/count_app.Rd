% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_app.R
\name{count_app}
\alias{count_app}
\title{Launch the interactive pollen counting application}
\usage{
count_app(...)
}
\arguments{
\item{...}{Arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}, e.g. \code{port = 3838}.}
}
\value{
Called for its side effect (launches the app). Does not return
until the app is stopped.
}
\description{
Opens the pcountr Shiny counting app in your default browser. The app
provides a keystroke-driven counting interface that mirrors the PCount DOS
workflow while saving to the native YAML format.
}
\section{Input syntax}{

Type any of the following in the input field and press Enter:
\describe{
\item{\code{B1}, \code{I80}, \code{A1}}{\strong{With preservation codes (default):} taxon code +
base preservation digit (1--8) + optional modifiers (\code{0} = half-grain,
\code{9} = hidden).}
\item{\code{B}, \code{I}, \code{A}}{\strong{Without preservation codes:} taxon code only (when
"Use preservation codes?" is set to No on setup). The stream displays grains
separated by \code{_}. Preservation columns are suppressed.}
\item{\code{.}}{One tracer microsphere (spike). Only relevant when concentration
method is "spike".}
\item{\verb{/label/}}{A traverse marker — stored verbatim.}
\item{\verb{[text]}}{An inline remark — stored verbatim.}
}
Use the \strong{New Slide} button to record a slide transition; you will be
prompted for the slide ID.
}


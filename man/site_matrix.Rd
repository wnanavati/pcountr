% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_matrix.R
\name{site_matrix}
\alias{site_matrix}
\title{Convert a loaded site to a wide taxa matrix}
\usage{
site_matrix(
  site,
  groups = NULL,
  taxon_label = c("name", "alias", "code"),
  min_present = 0L
)
}
\arguments{
\item{site}{A \code{pollen_site} with a \code{samples} list (from \code{\link[=read_site]{read_site()}}).}

\item{groups}{Character vector of dictionary group codes forming the
percentage denominator (e.g. \code{c("A","B","F")}). Defaults to
\code{site$pollen_sum}.}

\item{taxon_label}{Dictionary field for column names: \code{"name"} (default),
\code{"alias"}, or \code{"code"}. When \code{"alias"} is chosen and a taxon has no
alias, the code is used as fallback.}

\item{min_present}{Minimum number of samples a taxon must appear in (with
non-zero percentage) to be included. Default \code{0} keeps all taxa.}
}
\value{
A named list. Vectors are parallel to matrix rows (one entry per
sample, ordered shallowest first):
\describe{
\item{\code{DepTop}}{Top-of-interval depth (cm).}
\item{\code{DepBot}}{Bottom-of-interval depth (cm); \code{NA} where not recorded.}
\item{\code{AgeTop}}{Top-of-interval age (years BP); \code{NA} where not recorded.}
\item{\code{AgeBot}}{Bottom-of-interval age (years BP); \code{NA} where not recorded.}
\item{\code{SampleSize}}{Sample quantity (ml or g).}
\item{\code{SpikeCount}}{Number of spike markers counted.}
\item{\code{SpikeAdded}}{Spike tablets or volume added to the sample.}
\item{\code{SpikeConc}}{Concentration of the spike (markers per tablet or per ml).}
\item{\code{TaxaCount}}{Raw weighted counts matrix (samples x taxa).}
\item{\code{TaxaPerc}}{Percentage matrix (samples x taxa).}
\item{\code{TaxaConc}}{Concentration matrix (samples x taxa); \code{NA} cells
where required inputs are missing.}
\item{\code{TaxaAccRate}}{Accumulation rate matrix (samples x taxa); \code{NA}
cells where required inputs are missing.}
\item{\code{groups_used}}{Group codes used as the percentage denominator.}
}
}
\description{
Returns a list of parallel matrices and vectors suitable for downstream
analysis and plotting (e.g. \code{rioja::strat.plot()}). All non-special taxa
present in at least \code{min_present} samples are included.
}
\details{
Percentages (\code{TaxaPerc}) are computed as:
\deqn{p_{ij} = 100 \times w_{ij} / D_i}
where \eqn{w_{ij}} is the weighted count of taxon \eqn{j} in sample
\eqn{i} and \eqn{D_i} is the sum of all entry weights in \code{groups}.

Concentrations (\code{TaxaConc}) use the Stockmarr equation per taxon:
\deqn{c_{ij} = \frac{w_{ij}}{S_i} \times \frac{A_i \times \rho_i}{V_i}}
where \eqn{S_i} is spike counted, \eqn{A_i} is spike tablets/volume added,
\eqn{\rho_i} is spike concentration, and \eqn{V_i} is sample size.
Returns \code{NA} for any sample missing required inputs.

Accumulation rates (\code{TaxaAccRate}) are concentration divided by linear
deposition time: \eqn{(AgeBot - AgeTop) / (DepBot - DepTop)}.
Returns \code{NA} for any sample missing depth or age intervals.

Samples lacking a \code{depth_top} value are excluded; a message lists them.
}
\seealso{
\code{\link[=read_site]{read_site()}}, \code{\link[=set_metadata]{set_metadata()}}, \code{\link[=write_tlx]{write_tlx()}}
}

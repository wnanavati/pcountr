% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pollen_count.R
\name{pollen_count}
\alias{pollen_count}
\title{Construct a pollen_count object}
\usage{
pollen_count(
  grains,
  spike_n,
  traverses = character(0),
  remarks = list(),
  events = list(),
  sample_quantity = NA_real_,
  units = NA_character_,
  spike_tablets = NA_real_,
  spike_density = NA_real_,
  spike_units = NA_character_,
  pollen_sum_groups = c("A", "B", "F"),
  depth_top = NA_real_,
  depth_bottom = NA_real_,
  age_top = NA_real_,
  age_bottom = NA_real_,
  sample_name = NA_character_,
  dic_path = NA_character_,
  slide = NA_character_,
  title = NA_character_,
  source_file = NA_character_,
  conc_method = "spike",
  use_pres = TRUE,
  site = NULL
)
}
\arguments{
\item{grains}{Data frame of per-grain records (see \code{\link[=read_cnt]{read_cnt()}}).}

\item{spike_n}{Number of tracer microspheres counted.}

\item{traverses}{Character vector of traverse labels, in order.}

\item{remarks}{List of inline remarks.}

\item{events}{Ordered list of all count events.}

\item{sample_quantity}{Quantity of sediment processed.}

\item{units}{Unit of \code{sample_quantity} (\code{"ml"} or \code{"g"}).}

\item{spike_tablets, spike_density}{Quantity of spike added and microspheres
per unit (per tablet, per ml, or per g depending on \code{spike_units}).}

\item{spike_units}{Units of the spike added: \code{"tablets"}, \code{"ml"}, or \code{"g"}.}

\item{pollen_sum_groups}{Group codes forming the basic pollen sum.}

\item{depth_top, depth_bottom}{Sample depths (analyst-supplied).}

\item{age_top, age_bottom}{Sample ages in years BP (optional, analyst-supplied;
present = 1950 CE).}

\item{sample_name}{Analyst-assigned sample label (e.g. \code{"KF24sh#001"}).}

\item{dic_path}{Full path to the dictionary file used for this count
(stored so the counting app can auto-reload it on resume).}

\item{slide, title, source_file}{Provenance metadata.}

\item{conc_method}{Concentration calculation method: \code{"spike"} (tracer
equation, default), \code{"volumetric"} (counts divided by sample quantity),
or \code{"none"} (no concentration computed).}

\item{use_pres}{Logical; \code{TRUE} (default) means grains carry preservation
codes. \code{FALSE} means no preservation digit was recorded.}

\item{site}{Optional \code{\link[=pollen_site]{pollen_site()}}.}
}
\description{
The core per-sample object. Stores grains in counting order (a data frame),
the tracer-spike total, traverse labels, remarks, the full ordered event
list, and sample-level metadata including depth and (optional) age.
}
\details{
Depth and age are \emph{not} present in legacy \code{.CNT} files and must be supplied
by the analyst (here, or later via direct assignment). Age is only needed for
accumulation-rate arithmetic and may be left \code{NA}.
}

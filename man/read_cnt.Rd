% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cnt.R
\name{read_cnt}
\alias{read_cnt}
\title{Read a legacy PCount count file (.CNT)}
\usage{
read_cnt(path, site = NULL, quiet = FALSE)
}
\arguments{
\item{path}{Path to a \code{.CNT} file.}

\item{site}{Optional \code{\link[=pollen_site]{pollen_site()}}. If supplied, taxon codes are resolved
against its dictionary and unresolved codes are reported as anomalies.}

\item{quiet}{If \code{TRUE}, suppress the anomaly warning.}
}
\value{
A \code{pollen_count} object.
}
\description{
Parses the PCount \code{.CNT} format into a per-grain \code{pollen_count} object,
preserving the original counting order, traverse structure, tracer-spike
marks and inline remarks.
}
\details{
The validated grammar (confirmed against a full 20-sample site) is:
\itemize{
\item \strong{Header} -- 5 lines: program tag, timestamp, a config line
(\verb{dict, sample_qty, spike_tablets, spike_density, n0, units;}), the
pollen-sum definition (e.g. \verb{POLLEN SUM = ABF}) and a title line.
\item \strong{Slide description} -- text wrapped in \code{{ ... }}.
\item \strong{Grain} -- a taxon code (1-2 letters, optionally prefixed \verb{#}/\code{$})
followed by one base preservation digit \code{1}-\code{8}, optionally followed by
modifier digits drawn from \verb{\{0, 9\}} (where \code{0} marks a half-grain fragment,
weight 0.5, and \code{9} marks hidden).
\item \strong{Spike} -- a bare \code{.} (one tracer microsphere; no preservation digit).
\item \strong{Traverse marker} -- \verb{/label/} where \code{label} is any free text the
analyst chooses (stored verbatim; never validated).
\item \strong{Remark} -- free text in \verb{[ ... ]}, kept verbatim at its position.
}

Anything not matching these is recorded as an \emph{anomaly} (e.g. stray digits
from data-entry typos). Valid grains are still imported; anomalies are
attached as the \code{anomalies} attribute and reported via a warning.
}
\references{
Grimm, E. C. (1994). \emph{PCount} (Version 2.0) [MS-DOS software].
Illinois State Museum, Research and Collections Center, Springfield, IL.
}

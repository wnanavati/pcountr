% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tilia.R
\name{read_tilia_lookup}
\alias{read_tilia_lookup}
\title{Read a Tilia taxon lookup file}
\usage{
read_tilia_lookup(path = NULL, type = "pollen")
}
\arguments{
\item{path}{Directory holding the lookup files, or the full path to a single
\code{.xml} file. \code{NULL} (default) uses \code{getOption("pcountr.tilia_lookup")} if
set, otherwise \verb{C:/ProgramData/Tilia/Lookup}.}

\item{type}{Which lookup to read when \code{path} is a directory. One of
\code{"pollen"} (default), \code{"pollentrap"}, \code{"diatom"}, \code{"plantmacrofossil"},
\code{"phytolith"}, \code{"charcoal"}, \code{"macrocharcoal"}, \code{"microcharcoal"},
\code{"testateamoebae"}, \code{"ostracode"}, \code{"vertebrate"}. Ignored when \code{path}
points at a file.}
}
\value{
A \code{tilia_lookup} data frame: \code{taxon_id}, \code{name}, \code{author}, \code{code},
\code{taxa_group}, \code{ecol_group}, \code{higher_id}, plus the \code{synonyms} and \code{groups}
attributes described above.
}
\description{
Parses one of Tilia's Neotoma taxon lookup files into a data frame. This is a
pure reader: it returns what the file contains and makes no judgements about
it. Reconciliation against a dictionary is \code{\link[=standardize_dic]{standardize_dic()}}'s job.
}
\details{
Tilia stores its lookups as XML, one file per proxy, normally in
\verb{C:/ProgramData/Tilia/Lookup}. Each taxon carries a Neotoma taxon ID, an
accepted name, a Tilia display abbreviation, a three-letter \code{TaxaGroup}
(the organism or proxy dimension) and a four-letter \code{EcolGroup} (the
ecological dimension within it) -- for example \code{TRSH} trees and shrubs,
\code{UPHE} upland herbs, \code{VACR} terrestrial vascular cryptogams, \code{AQVP} aquatic
vascular plants.

Note that the \code{Code} field holds Tilia's \emph{display} abbreviation (\code{Ane.s.},
\code{Pla.sp1LLC}) and not a keystroke code. Counting codes are one or two
characters chosen by the analyst and belong in the dictionary, not here.
}
\section{What is returned}{

A data frame of accepted taxa, with two attributes:
\describe{
\item{\code{synonyms}}{Data frame of \code{name} and \code{taxon_id}: deprecated names
mapped to the accepted taxon they now refer to. This is Neotoma's own
synonymy, so matches drawn from it are authoritative rather than
guessed.}
\item{\code{groups}}{Data frame of the \code{TaxaGroup} / \code{EcolGroup} hierarchy
declared at the head of the file, with names.}
}
}

\section{Size}{

The pollen file is around 11 MB and holds roughly 49,000 taxa, because it
contains the entire Neotoma taxonomy rather than only palynomorphs. Parsing
takes a few seconds; the result is cached for the session, keyed on the file's
path and modification time. \code{\link[=standardize_dic]{standardize_dic()}} restricts matching to
palynomorph \code{TaxaGroup}s by default.
}

\seealso{
\code{\link[=standardize_dic]{standardize_dic()}}, \code{\link[=read_dic]{read_dic()}}
}

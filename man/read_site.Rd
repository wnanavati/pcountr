% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_loader.R
\name{read_site}
\alias{read_site}
\title{Load an entire site folder of count files}
\usage{
read_site(
  folder,
  dic = NULL,
  name = basename(folder),
  metadata = NULL,
  col_map = NULL,
  ignore_depth_conflicts = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{folder}{Path to a folder containing count files (and optionally a
\code{.DIC} dictionary).}

\item{dic}{Path to a \code{.DIC} file, or a pre-loaded \code{pollen_dictionary}.
Auto-detected when exactly one \code{.DIC} exists in \code{folder}; an error is
raised if zero or more than one are found.}

\item{name}{Site name. Defaults to the folder's basename.}

\item{metadata}{Optional path to a CSV metadata sheet.}

\item{col_map}{Named character vector mapping standard field names to your
sheet's actual column names. Standard names: \code{file}, \code{depth_top},
\code{depth_bottom}, \code{age_top}, \code{age_bottom}. Example:
\code{col_map = c(file = "Sample_ID", depth_top = "Top_cm", depth_bottom = "Bot_cm")}. Column matching is case-insensitive.}

\item{ignore_depth_conflicts}{If \code{TRUE}, warn instead of error when a
YAML-embedded depth disagrees with the sheet; the YAML value is kept.}

\item{quiet}{If \code{TRUE}, suppress per-file anomaly warnings.}
}
\value{
A \code{pollen_site} with a \code{samples} list ordered by depth.
}
\description{
Discovers every \code{.CNT} and \code{.yaml}/\code{.yml} file in \code{folder}, loads them as
\code{pollen_count} objects, optionally attaches depth and age from a metadata
sheet, and returns a \code{pollen_site} with all samples ordered by \code{depth_top}
(samples lacking a depth are appended at the end, unordered).
}
\details{
Depth and age are \emph{never required} to load a site. Samples without depths
load and compute concentrations exactly as normal; they simply cannot be
ordered or plotted by depth until one is supplied.
}
\section{Depth and age sources (all optional)}{

\enumerate{
\item Embedded in native \code{.yaml} files -- the primary route going forward.
\item An optional CSV metadata sheet (\code{metadata} + \code{col_map}).
\item Direct assignment with \code{\link[=set_metadata]{set_metadata()}}.
}
When a \code{.yaml} file already carries a depth value \emph{and} the sheet also
provides one for the same sample, the two values must agree. A discrepancy
is an error unless \code{ignore_depth_conflicts = TRUE} (which downgrades it to a
warning and keeps the YAML value).
}

\section{Metadata sheet}{

A CSV with one row per sample. Rows are matched to files case-insensitively
and without regard to extension (\code{LMSH001} matches \code{LMSH001.CNT},
\code{lmsh001.yaml}, etc.). Columns for sample quantity, units, and spike are
ignored -- those are always read from the count file itself.

Supply your own column names via \code{col_map} (see below). When the sheet is
provided, folder files with no matching row and sheet rows with no matching
file both produce warnings, not errors.

Depth intervals must satisfy \code{depth_top < depth_bottom} (depth increases
downcore). Rows where \code{depth_top >= depth_bottom} are flagged as likely
transcription swaps.
}


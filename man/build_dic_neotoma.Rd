% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neotoma.R
\name{build_dic_neotoma}
\alias{build_dic_neotoma}
\title{Build a starting dictionary from nearby Neotoma records}
\usage{
build_dic_neotoma(
  lat,
  long,
  radius_km = 250,
  max_sites = 20,
  datasettype = "pollen",
  n = 50,
  file = NULL
)
}
\arguments{
\item{lat, long}{Latitude and longitude in decimal degrees (WGS 84).}

\item{radius_km}{Search radius. The default is generous on purpose --
a smaller box is no faster.}

\item{max_sites}{Maximum number of nearest sites to download. This is the
knob that bounds cost.}

\item{datasettype}{A Neotoma datasettype, passed through verbatim; see
\code{neotoma2::get_table("datasettypes")}. Charcoal types carry no taxon
vocabulary and will yield a near-empty result.}

\item{n}{Number of taxa to return.}

\item{file}{Optional path; when given, the draft is written as CSV.}
}
\value{
A \code{data.frame} with the columns \code{\link[=read_dic_csv]{read_dic_csv()}} expects
(\code{code}, \code{alias}, \code{group}, \code{name}, \code{is_special}, \code{value}), invisibly if
\code{file} is supplied. \code{code} is blank throughout except for the spike.
}
\description{
Finds the Neotoma records nearest a coordinate, tallies which taxa occur
most widely among them, and returns a starting point for a dictionary. It
is deliberately a \emph{draft}: entry codes are left blank, because codes are
the analyst's own muscle memory and nobody should inherit someone else's.
}
\details{
Taxa are ranked by the number of distinct \strong{sites} they occur in, not by
abundance. A taxon present in one site but two hundred of its samples is a
local peculiarity; a taxon present at twelve sites is what an analyst
should expect to meet down the microscope. Ranking by abundance would
return little beyond \emph{Pinus}, \emph{Artemisia} and Poaceae.

Proximity is used rather than a political boundary because vegetation does
not stop at state lines. A 250 km search around a site in western Montana
reaches into Idaho and Wyoming and finds more relevant neighbours than the
state would.
}
\section{What this is not}{

The result is \strong{not} a \code{pollen_dictionary} and will not load until you
fill in the \code{code} column -- \code{\link[=read_dic_csv]{read_dic_csv()}} drops rows with a blank
code. Groups are imported from Neotoma's \code{ecologicalgroup} as a starting
value; they encode your pollen-sum decisions, so edit them freely. Check
additions against a Tilia lookup afterwards with \code{\link[=standardize_dic]{standardize_dic()}}.
}

\section{Cost}{

One slow spatial query, then one download per dataset among the
\code{max_sites} nearest sites. Expect the whole call to take \strong{a few
minutes}, depending on the radius and how many records lie within it.
Run it once per site; it is not something to loop over. Requires the
\strong{neotoma2} package and a network connection.
}

\seealso{
\code{\link[=standardize_dic]{standardize_dic()}}, \code{\link[=read_dic_csv]{read_dic_csv()}}
}
